'''学习使用 OpenCV 绘制不同几何图形• 你将会学习到这些函数：cv2.line()， cv2.circle()， cv2.rectangle()，cv2.ellipse()， cv2.putText() 等'''import cv2import numpy as np'''所有的这些绘图函数需要设置下面这些参数：    • img：你想要绘制图形的那幅图像。    • color：形状的颜色。以 RGB 为例，需要传入一个元组，例如： （255,0,0）代表蓝色。对于灰度图只需要传入灰度值。    • thickness：线条的粗细。如果给一个闭合图形设置为 -1，那么这个图形就会被填充。默认值是 1.    • linetype：线条的类型， 8 连接，抗锯齿等。默认情况是 8 连接。 cv2.LINE_AA为抗锯齿，这样看起来会非常平滑。'''# 画线def draw_line():    '''    功能： 绘制一条直线    函数： line    '''    img = np.zeros((512, 512, 3), np.uint8)    # 图片绘制一条直线    cv2.line(img, (0, 0), (511, 511), (255, 0, 0), 5)    cv2.imshow('img', img)    cv2.waitKey()    cv2.destroyAllWindows()    pass# 画矩形def draw_rectangle():    '''    功能： 要画一个矩形，    函数： cv2.rectangle    参数： 你需要告诉函数的左上角顶点和右下角顶点的坐标    '''    img = np.zeros((512, 512, 3), np.uint8)    # 图片绘制一条直线    cv2.rectangle(img, (340, 0), (511, 128), (255, 0, 0), 5)    cv2.imshow('img', img)    cv2.waitKey()    cv2.destroyAllWindows()    pass# 画圆def draw_circle():    '''    功能： 要画一个圆    函数： cv2.circle    参数： 需要指定圆形的中心点坐标和半径大小。    '''    img = np.zeros((512, 512, 3), np.uint8)    # 实心圆    cv2.circle(img, (447, 63), 63, (255, 0, 0), -1)    cv2.imshow('img', img)    cv2.waitKey()    cv2.destroyAllWindows()    pass# 画椭圆def draw_ellipse():    '''    功能： 要画一个椭圆    函数： cv2.ellipse    参数： ( 中心点的位置坐标, 长轴和短轴的长度, 轴，椭圆沿逆时针方向旋转的角度 )    椭圆弧沿顺时针方向起始的角度和结束角度，如果是 0  360，就是整个椭圆。    '''    img = np.zeros((512, 512, 3), np.uint8)    # 椭圆，x轴逆时针180度， 实心    cv2.ellipse(img, (256, 256), (100, 50), 0, 0, 180, (255, 0, 0), -1)    cv2.imshow('img', img)    cv2.waitKey()    cv2.destroyAllWindows()    pass'''在图片上添加文字    font=cv2.FONT_HERSHEY_SIMPLEX2 cv2.putText(img,'OpenCV',(10,500), font, 4,(255,255,255),2)'''# 在图片上输出文字def put_text():    '''       功能： 图片上绘制文字       函数： cv2.putText       参数： ( 你要绘制的文字, 你要绘制的位置, 字体类型, 字体的大小, 文字的一般属性如颜色，粗细，线条的类型)                为了更好看一点推荐使用linetype=cv2.LINE_AA。       '''    img = np.zeros((512, 512, 3), np.uint8)    font = cv2.FONT_HERSHEY_SIMPLEX    cv2.putText(img, 'OpenCV', (10, 500), font, 4, (255, 255, 255), 2)    cv2.imshow('img', img)    cv2.waitKey()    cv2.destroyAllWindows()    passif __name__ == '__main__':    put_text()    pass