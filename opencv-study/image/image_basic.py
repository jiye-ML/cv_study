'''图像的基本操作读入, 显示, 写获取像素值并修改• 获取图像的属性（信息）• 图像的 ROI（）• 图像通道的拆分及合并'''import cv2'''读入图像'''def read_image():    '''    功能： 读取图像数据返回    参数： 告诉函数应该如何读取这幅图片。            • cv2.IMREAD_COLOR：读入一副彩色图像。图像的透明度会被忽略，这是默认参数。            • cv2.IMREAD_GRAYSCALE：以灰度模式读入图像            cv2.IMREAD_UNCHANGED：读入一幅图像，并且包括图像的 alpha 通道    '''    return cv2.imread('../data/plane.jpg')    pass'''显示图像使用函数 cv2.imshow() 显示图像。窗口会自动调整为图像大小。第一个参数是窗口的名字，其次才是我们的图像。你可以创建多个窗口，只要你喜欢，但是必须给他们不同的名字cv2.imshow('image',img)cv2.waitKey(0)cv2.destroyAllWindows()cv2.waitKey() 是一个键盘绑定函数。需要指出的是它的时间尺度是毫秒级。函数等待特定的几毫秒，看是否有键盘输入。特定的几毫秒之内，如果按下任意键，这个函数会返回按键的 ASCII 码值，程序将会继续运行。如果没有键盘输入，返回值为 -1，如果我们设置这个函数的参数为 0，那它将会无限期的等待键盘输入。它也可以被用来检测特定键是否被按下，例如按键 a 是否被按下，这个后面我们会接着讨论。cv2.destroyAllWindows() 可以轻易删除任何我们建立的窗口。如果你想删除特定的窗口可以使用 cv2.destroyWindow()，在括号内输入你想删除的窗口名。建议： 一种特殊的情况是，你也可以先创建一个窗口，之后再加载图像。这种情况下，你可以决定窗口是否可以调整大小。使用到的函数是 cv2.namedWindow()。初始设定函数标签是 cv2.WINDOW_AUTOSIZE。但是如果你把标签改成cv2.WINDOW_NORMAL，你就可以调整窗口大小了。当图像维度太大，或者要添加轨迹条时，调整窗口大小将会很有用cv2.namedWindow('image', cv2.WINDOW_NORMAL)cv2.imshow('image',img)cv2.waitKey(0)cv2.destroyAllWindows()''''''保存图像cv2.imwrite('messigray.png',img)彩色图像使用 OpenCV 加载时是 BGR 模式。但是 Matplotib 是 RGB模式。所以彩色图像如果已经被 OpenCV 读取，那它将不会被 Matplotib 正确显示。''''''获取并修改像素值'''def modify_image(image):    # 获得第一个通道数据    blue = image[:, :, 0]    image[100, 100, :] = [255, 255, 255]    pass'''获取图像属性'''def attribute(image):    '''    图像的属性包括：行，列，通道，图像数据类型，像素数目等    '''    # 形状    print(image.shape)    # 像素的数目    print(image.size)    # 数据类型    print(image.dtype)    pass'''图像 ROI'''def roi(image):    '''    有时你需要对一幅图像的特定区域进行操作。例如我们要检测一副图像中眼睛的位置，    我们首先应该在图像中找到脸，再在脸的区域中找眼睛，而不是直接在一幅图像中搜索。这样会提高程序的准确性和性能    '''    ball = image[280:340, 330:390, :]    image[273:333, 100:160] = ball    pass'''拆分及合并图像通道'''def split(image):    b, g, r = cv2.split(image)    image = cv2.merge(b, g, r)    passif __name__ == '__main__':    image = read_image()    attribute(image)    pass